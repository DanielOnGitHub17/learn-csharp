// generate initial main structure
using System;

namespace play
{
    class Program
    {
        static void Main(string[] args)
        {
            // foreach (var arg in args){
            //     Console.Write($"{arg} ");
            // }
            // Console.WriteLine("\nHello World!");
            long i = 0;
            while (i < Math.Pow(10, 9)){
                i++;
            }
            Console.WriteLine("Done!");
        }
    }
}
